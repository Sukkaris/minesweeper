# マインスイーパー PWA — プロジェクト共通前提

このファイルはリポジトリ直下に置き、Claude Code が自動で読み込む前提の指示書です。
以下の内容は、すべての作業段階で常に適用されます。

## 概要
iPhone で遊ぶ個人用マインスイーパーを、PWA（Progressive Web App）として実装する。
GitHub Pages で配信し、Safari から「ホーム画面に追加」して起動する。
インストール後は完全にオフラインで動作すること。

## 対象端末
iPhone 17 のみを対象とする。
- CSS ビューポート: 402 × 874 pt
- デバイスピクセル比: 3
- セーフエリア: 上部 約59pt（Dynamic Island）、下部 約34pt

他機種への対応は不要だが、幅・高さを固定値で決め打ちせず、
ビューポートから算出する実装にすること。

## 技術的制約
- 素の HTML / CSS / JavaScript のみで実装する
- ビルドツール、トランスパイラ、パッケージマネージャは一切使わない
- 外部ライブラリ、CDN、Web フォントへの依存を禁止する（オフライン動作のため）
- 画像素材を外部ファイルとして持たない。アイコンを除き、装飾はすべて CSS または
  インライン SVG で表現する
- 縦画面専用。横画面は考慮しない
- 対象は iOS Safari のみ。他ブラウザ向けの互換コードは不要

## 配信先とパス指定（重要）
GitHub Pages のサブディレクトリ配信を行う。
- リポジトリ名: minesweeper
- 配信 URL: https://<ユーザー名>.github.io/minesweeper/

したがって、**すべてのパス指定を相対パスで記述すること**。
絶対パス（/css/style.css のような先頭スラッシュ）を一切使ってはならない。
- HTML 内の参照: ./css/style.css
- Service Worker の登録: navigator.serviceWorker.register('./sw.js')
- Service Worker のキャッシュ対象列挙: './', './index.html', './css/style.css' など
- manifest.json の start_url と scope: "./"

絶対パスを使うと、ローカルでは動作するのに GitHub Pages 上で
ファイルが取得できず真っ白になる。この事故を必ず回避すること。

## ファイル構成
minesweeper/
├── index.html      … 構造のみ。ロジックとスタイルを含めない
├── css/
│   ├── style.css   … レイアウト、共通スタイル、CSS変数の定義
│   └── themes.css  … テーマごとの変数値の上書きのみ
├── js/
│   ├── game.js     … 盤面データ、地雷配置、開放処理、勝敗判定（DOM に触れない）
│   ├── solver.js   … 無推測判定ソルバー（第4段階で追加）
│   └── ui.js       … DOM 操作、イベント処理、描画
├── manifest.json
├── sw.js           … Service Worker
├── icons/
│   ├── icon.svg
│   ├── apple-touch-icon.png  (180×180)
│   ├── icon-192.png
│   └── icon-512.png
└── tools/
    └── generate-icons.html   … アイコン生成用（キャッシュ対象外）

## 設計方針
- game.js はゲームのロジックのみを持ち、DOM を一切参照しない。
  UI から独立してテスト可能な状態を保つ
- 色・寸法・フォントはすべて CSS カスタムプロパティとして :root に定義する。
  値を直接書き込まない。テーマ追加が変数の上書きだけで完結する構造にする
- テーマは <html data-theme="..."> の属性値で切り替える
- 状態の永続化は localStorage を使う。キーには "ms-" の接頭辞を付ける
- 調整の可能性がある数値（長押し閾値、試行上限、密度の閾値など）は、
  コード中に散らさず定数としてまとめて定義する

## Service Worker のバージョン更新（第2段階以降、毎回必須）
sw.js が既に存在する状態でコードを変更した場合、
**作業の最後に必ず sw.js のキャッシュバージョン番号を上げること。**
これを忘れると、ホーム画面のアプリは古いキャッシュで起動し続け、
変更が一切反映されない。

作業完了の報告時に、バージョン番号を上げたかどうかを明記すること。

## コーディング規約
- ES Modules（import / export）を使う
- 日本語のコメントを適度に入れる
- 変数名・関数名は英語
- 1ファイルが 500 行を超えそうな場合は、その旨を報告して分割方針を相談すること
