# Minesweeper（iPhone 用 PWA）

iPhone の Safari から「ホーム画面に追加」して遊ぶ、個人用のマインスイーパーです。
素の HTML / CSS / JavaScript だけで作られており、インストール後は完全にオフラインで動作します。

- 公開 URL: https://sukkaris.github.io/minesweeper/
- 難易度の切り替え（暫定）: URL の末尾に `?level=beginner` / `?level=intermediate` / `?level=advanced` を付けて開く。
  一度開くとその難易度が保存され、ホーム画面から起動したときも同じ難易度になります。

---

## 1. アイコンの生成手順

アイコンの元データは `icons/icon.svg`（地雷のシルエット）です。
これを PNG に変換したものが `icons/` に置いてあります。意匠を変えたときは次の手順で作り直します。

1. `tools/generate-icons.html` をブラウザで開く
   - ローカルサーバー経由で開くと `icon.svg` が自動で読み込まれます
   - ファイルを直接（`file://` で）開いた場合は自動読み込みに失敗するので、
     画面のテキストエリアに `icon.svg` の中身を貼り付けて「貼り付けた SVG を使う」を押す
2. 「3 サイズをまとめてダウンロード」を押す（または各サイズの「ダウンロード」を押す）
3. ダウンロードされた 3 つのファイルを `icons/` フォルダに上書きで置く
   - `apple-touch-icon.png`（180×180）
   - `icon-192.png`（192×192）
   - `icon-512.png`（512×512）

`tools/` フォルダは開発用なので、Service Worker のキャッシュ対象には含まれていません。

---

## 2. コードを更新したら必ずやること（重要）

**`sw.js` の先頭にあるバージョン番号を 1 つ増やしてください。**

```js
const CACHE_VERSION = 1;   // ← ここを 2, 3, 4 … と増やす
```

このアプリは、一度ホーム画面に入れると、ファイルを端末内に丸ごと保存して
そこから起動します（だからオフラインで動きます）。
バージョン番号が同じままだと「新しいファイルがある」と気付けず、
GitHub に push しても **ホーム画面のアプリは古いまま** です。

番号を増やして push すると、次にアプリを起動したときに画面上部に
「新しいバージョンがあります ／ 再読み込み」のバナーが出ます。
バナーを押すと新しい版に切り替わります。押さなければ、遊んでいる途中の盤面が
消えることはありません。アプリを完全に終了して次に起動したときは、
新しい版に切り替わった状態で開くか、再びバナーが出ます。

---

## 3. GitHub Pages の有効化手順

1. GitHub のリポジトリページを開く
2. 上のタブから **Settings** を開く
3. 左のメニューから **Pages** を選ぶ
4. 「Build and deployment」の **Source** を「Deploy from a branch」にする
5. **Branch** で `main` と `/ (root)` を選んで **Save**
6. 数分待つと `https://<ユーザー名>.github.io/minesweeper/` で公開されます

すべてのファイルは相対パスで書かれているので、サブディレクトリ配信でもそのまま動きます。

---

## 4. iPhone での導入手順

1. iPhone の **Safari** で公開 URL を開く（他のブラウザでは「ホーム画面に追加」ができません）
2. 画面下の **共有ボタン**（四角から矢印が出ているアイコン）を押す
3. メニューから **「ホーム画面に追加」** を選ぶ
4. 名前が「Minesweeper」になっていることを確認して **追加**
5. ホーム画面に地雷のアイコンが現れるので、そこから起動する

ホーム画面から起動すると、Safari のアドレスバーが無い全画面のアプリとして動きます。

---

## 5. 動作確認のチェックリスト

導入直後や更新後に、以下を確認してください。

- [ ] ホーム画面のアイコンが地雷のシルエットになっている
- [ ] ホーム画面から起動すると、アドレスバーの無い全画面で開く
- [ ] ヘッダーが Dynamic Island と重ならず、フッターがホームインジケータと重ならない
- [ ] **機内モードにしてから**ホーム画面のアイコンで起動でき、最後まで遊べる
- [ ] マスを押している間、指の下のマスが沈み込んで見える。指をずらすと沈むマスが移動する
- [ ] 盤面の外まで指を動かして離すと、何も開かない（キャンセル）
- [ ] 長押しが成立した瞬間、マスの縁が一瞬光る
- [ ] フッターのボタンで開放モード／フラグモードが切り替わり、フラグモードではフッターが赤系になる
- [ ] 数字マスをタップすると、周囲の旗が数字と一致する場合に一斉に開く（チョーディング）
- [ ] 設定（歯車）で長押し時間を変えると、試し押しのマスでその長さが体感できる
- [ ] 長押し中にテキスト選択やメニューが出ない。ダブルタップしても拡大しない
- [ ] `sw.js` のバージョンを上げて push した後、アプリを起動すると更新バナーが出る。
      押すと新しい版になり、押さなければ盤面はそのまま
- [ ] 無推測モード（設定で既定オン）のまま上級を最後まで遊ぶと、50% の賭けを強いられる場面が出ない
- [ ] カスタムで地雷密度を 25% 以上にすると「密度が高いため無推測モードは既定で無効です」と出る。
      そこで「それでも無推測モードを使う」をオンにして始めると、見つからなかったときに
      「無推測の盤面を生成できませんでした」の通知が数秒出て、通常の盤面で始まる
- [ ] 設定のテーマで 7 種（クラシック / ダーク / ミニマル / ターミナル / 木目 / ネオン / ペーパー）が
      切り替わり、どのテーマでも数字 1〜8 が読み分けられる。アプリを再起動しても選んだテーマのまま
- [ ] ペーパーの数字が手書き風の書体（Bradley Hand）で出て、マスごとに僅かに傾いている（実機でのみ確認可能）
- [ ] 空白をタップすると、開くマスが中心から波紋のように広がる。
      地雷を踏むと、踏んだマスが光った後に他の地雷が近い順に現れ、間違った旗に × が付く。
      クリアすると残りのマスに順に旗が立ち、盤面が一瞬明るくなる
- [ ] 「開放・終了時の演出」をオフにすると上記が即座の表示になる。押したときの沈み込みはオフでも残る
- [ ] サウンドをオンにすると、開放・旗・クリア・失敗で短い電子音が鳴る（本体の消音スイッチが入っていると鳴らない）

---

## 6. 無推測モードの仕組みと調整

初手をタップした瞬間に、地雷の配置をランダムに作っては「数字だけで最後まで解けるか」を
`js/solver.js` で判定し、解ける配置が出るまで作り直しています（上限あり）。
判定に使う推論は 3 種類だけ（1 マスの数字・隣り合う数字の差・残り地雷数）で、
それで解けない盤面は「運が必要」とみなして捨てます。

調整できる数値は `js/solver.js` の先頭 `NO_GUESS` にまとめてあります。

| 項目 | 現在値 | 意味 |
| --- | --- | --- |
| `attemptLimits` | 初級 200 / 中級 500 / 上級 1000 / カスタム 2000 | 作り直しの上限。超えたら通常の盤面で始める |
| `highDensityThreshold` | 0.24 | 地雷密度がこれを超えるカスタムは無推測を既定オフにする |
| `highDensityAttemptLimit` | 100 | 高密度で明示的にオンにしたときの上限（待ち時間を 1 秒未満に） |

2026-09 の実測（1 試行の所要時間と成功率）: 上級は 1 回 0.22ms・成功率 5% で、
1000 回の上限に達することは事実上ありません。大きな盤面で密度 25% になると成功率が 0% になるため、
閾値は指示書の 25% から 24% に下げました。生成にかかった時間は、開発者ツールのコンソールに
`[no-guess] …` の行として毎回出力されます。

---

## 7. テーマ・演出・サウンドの調整

### テーマ
テーマは `css/themes.css` に「変数の上書き」だけで定義されています。
色を変えたいときはそのテーマのブロック内の `--color-*` を書き換えるだけで、
盤面・ヘッダー・設定画面のすべてに反映されます（レイアウトは `css/style.css`、マスの形状と演出は `css/board.css` にあります）。

- 旗と地雷の形は全テーマ共通で、色だけが変わります。例外はターミナル（⚑ と ✳）と木目（朱印と焼き印）で、
  この 2 つだけ `index.html` の SVG に別の形を持ち、`--icon-flag-*` / `--icon-mine-*` で切り替えています。
- ペーパーの数字の傾き・ずれは、マスの位置から決まる固定の値です（`ui.js` の `cellSeed`）。
  傾きの強さは `--num-tilt-max` / `--num-shift-max` で調整できます。
- 「視差効果を減らす」（iPhone の設定 → アクセシビリティ → 動作）が有効な端末では、
  演出トグルの初期値がオフになります。一度トグルを触ればその値が優先されます。

### 演出の時間
`js/effects.js` の先頭の定数で調整します。

| 定数 | 現在値 | 意味 |
| --- | --- | --- |
| `RIPPLE_STEP_MS` | 36 | 波紋：起点から 1 マス離れるごとの遅れ |
| `RIPPLE_MAX_MS` | 700 | 波紋：全体がこれより長くなるときは間隔を詰める |
| `LOSS_MINES_START_MS` | 260 | 敗北：踏んだ地雷の強調から、他の地雷の開示を始めるまで |
| `LOSS_MINES_TOTAL_MS` | 1200 | 敗北：地雷を出し終えるまでの目安（上級 99 個でもこの程度） |
| `WIN_FLAGS_TOTAL_MS` | 600 | 勝利：旗を立て終えるまでの目安 |

### サウンド
`js/sound.js` の先頭の定数で音程・長さ・音量を調整します。音声ファイルは使わず、その場で合成しています。
`REVEAL_MIN_INTERVAL_MS`（45ms）より短い間隔の開放音は間引かれるので、連鎖開放でも音が重なりすぎません。
